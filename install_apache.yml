---
- hosts: all
  become: true
  tasks:
    - name: Install Apache and PHP packages
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: latest
